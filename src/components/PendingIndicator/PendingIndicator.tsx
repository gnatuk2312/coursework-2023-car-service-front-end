import { FC } from "react";

import { CircularProgress } from "./PendingIndicator.styles";

const PendingIndicator: FC = () => {
  return <CircularProgress />;
};

export default PendingIndicator;
